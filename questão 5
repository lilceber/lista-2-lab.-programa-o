#include <stdio.h>
int main() {
 int x,maior = -100000,menor = 100000;
 do {
    printf("digite um valor: ");
    scanf("%d",&x);
    
  if(x > maior ){
    maior = x;
 } if (x < menor){
    menor = x;
 }
    
}while (x!=0);
  printf("\nMaior: %d\nMenor: %d",maior,menor);
    return 0;
}
